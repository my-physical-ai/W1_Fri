# 우분투 한글 설정 노하우 레퍼런스
# Ubuntu Korean Setup Know-How Reference
# 최초 작성: 2025-02-16

---

## 프로젝트 개요
우분투(Ubuntu) 및 라즈베리파이 OS에서 한글 깨짐 해결, 한글 입력기 설정, 
환경변수 구성 등 한글 관련 이슈와 해결 과정을 기록한다.

---

## [소기능 1] 라즈베리파이5 한글 깨짐 해결 (IBus 방식)
- **날짜**: 2025-02-14
- **환경**: Raspberry Pi 5 / Ubuntu Desktop

### 발생한 문제
1. 한글 폰트 미설치로 한글이 □□□로 깨져 보임
2. ibus-hangul 설치 후 터미널에서는 한글 입력 가능하나, 브라우저(Chromium/Firefox)에서 한글 입력 불가
3. `~/.bashrc`에 환경변수를 넣었으나 GUI 앱에 적용되지 않음

### 해결 과정
1. **한글 폰트 설치**로 깨짐 해결
   ```bash
   sudo apt install -y fonts-nanum fonts-noto-cjk
   ```
2. **IBus 입력기 설치**
   ```bash
   sudo apt install -y ibus ibus-hangul
   ```
3. **핵심 이슈: ~/.bashrc vs ~/.xsessionrc 차이**
   - `~/.bashrc` → 터미널 세션에서만 환경변수 적용됨
   - `~/.xsessionrc` → 모든 데스크탑 GUI 앱에 환경변수 적용됨 ✅
   - 브라우저는 GUI 앱이므로 `~/.xsessionrc`에 설정해야 함

4. **~/.xsessionrc에 환경변수 설정** (IBus 방식)
   ```bash
   export GTK_IM_MODULE=ibus
   export QT_IM_MODULE=ibus
   export XMODIFIERS=@im=ibus
   ```

5. **XMODIFIERS 누락 이슈**
   - `GTK_IM_MODULE`과 `QT_IM_MODULE`만 설정하고 `XMODIFIERS`를 빼먹으면 
     일부 앱(특히 브라우저)에서 한글 입력이 안 됨
   - 반드시 3개 환경변수를 모두 설정해야 함

### 최종 해결 패턴 (IBus)
```bash
# 1. 패키지 설치
sudo apt update
sudo apt install -y fonts-nanum fonts-noto-cjk ibus ibus-hangul language-pack-ko

# 2. 로케일 설정
sudo locale-gen ko_KR.UTF-8
sudo update-locale LANG=ko_KR.UTF-8

# 3. ~/.xsessionrc 환경변수 (IBus)
export GTK_IM_MODULE=ibus
export QT_IM_MODULE=ibus
export XMODIFIERS=@im=ibus

# 4. 재부팅 (2번: 입력기 설치 후 1번, 환경변수 설정 후 1번)
sudo reboot
```

### 핵심 교훈
- **~/.bashrc는 터미널 전용**, GUI 앱에는 ~/.xsessionrc 사용 필수
- **XMODIFIERS=@im=ibus** 가 빠지면 브라우저에서 한글 입력 불가
- 재부팅은 총 2번 필요할 수 있음 (설치 후 + 환경변수 설정 후)

---

## [소기능 2] 우분투 한글 깨짐 해결 (Fcitx 방식)
- **날짜**: 2025-02-16
- **환경**: Ubuntu Desktop (일반 PC / 라즈베리파이 공통)

### 발생한 문제
- 우분투 기본 설치 시 한글 로케일, 폰트, 입력기가 없어 한글이 깨짐
- IBus 대비 더 가볍고 안정적인 입력기 필요

### IBus vs Fcitx 비교
| 항목 | IBus | Fcitx |
|------|------|-------|
| 속도 | 보통 | 더 빠르고 가벼움 |
| 한영 전환 안정성 | 가끔 충돌 | 안정적 |
| 우분투 기본 탑재 | O | X (별도 설치) |
| 다양한 앱 호환성 | 보통 | 우수 |

### 해결 과정 (Fcitx)
1. 언어팩, 로케일, 폰트 설치는 IBus 방식과 동일
2. **Fcitx 입력기 설치 & 기본 입력기 변경**
   ```bash
   sudo apt install -y fcitx-hangul
   im-config -n fcitx
   ```
3. **~/.xsessionrc 환경변수** (Fcitx 방식)
   ```bash
   export GTK_IM_MODULE=fcitx
   export QT_IM_MODULE=fcitx
   export XMODIFIERS=@im=fcitx
   ```
4. 재부팅 후 `fcitx-configtool`에서 Hangul 추가
   - "Only Show Current Language" 체크 해제 후 Hangul 검색

### Fcitx 설정 시 주의사항
- `fcitx-configtool`에서 Hangul이 안 보이면 → `sudo apt install --reinstall fcitx-hangul`
- Fcitx 트레이 아이콘이 안 나타나면 → `~/.xsessionrc`에 `fcitx-autostart &` 추가
- 한영 전환 키: `Ctrl + Space` 또는 `한/영` 키

### 최종 해결 패턴 (Fcitx)
```bash
# 1. 패키지 설치
sudo apt update
sudo apt install -y language-pack-ko fonts-nanum fonts-noto-cjk fcitx-hangul

# 2. 로케일 설정
sudo locale-gen ko_KR.UTF-8
sudo update-locale LANG=ko_KR.UTF-8

# 3. 기본 입력기를 Fcitx로 변경
im-config -n fcitx

# 4. ~/.xsessionrc 환경변수 (Fcitx)
cat << 'EOF' >> ~/.xsessionrc
export GTK_IM_MODULE=fcitx
export QT_IM_MODULE=fcitx
export XMODIFIERS=@im=fcitx
EOF

# 5. 재부팅
sudo reboot

# 6. 재부팅 후 Fcitx 설정에서 Hangul 추가
fcitx-configtool
```

### 핵심 교훈
- Fcitx는 IBus보다 가볍고 한영 전환이 안정적
- `im-config -n fcitx`로 시스템 기본 입력기를 반드시 변경해야 함
- 환경변수 3개(`GTK_IM_MODULE`, `QT_IM_MODULE`, `XMODIFIERS`)는 IBus든 Fcitx든 반드시 모두 설정

---

## 공통 핵심 노하우 요약

### 1. 환경변수 설정 파일 선택 (매우 중요!)
| 파일 | 적용 범위 | 사용 시점 |
|------|-----------|-----------|
| `~/.bashrc` | 터미널 세션만 | 터미널에서만 필요한 설정 |
| `~/.xsessionrc` | 모든 GUI 앱 (브라우저, 에디터 등) | 입력기 환경변수 필수 ✅ |
| `/etc/default/locale` | 시스템 전체 로케일 | 언어 설정 |

### 2. 한글 설정 3요소 (반드시 모두 필요)
| 요소 | 역할 | 없으면? |
|------|------|---------|
| 로케일 (`ko_KR.UTF-8`) | 한국어 규칙 | 시스템이 한국어를 모름 |
| 폰트 (`fonts-nanum` 등) | 글자 모양 | □□□로 깨짐 |
| 입력기 (`ibus-hangul` / `fcitx-hangul`) | 키보드 한글 입력 | 한글 타이핑 불가 |

### 3. 환경변수 3개 (하나라도 빠지면 문제 발생)
```bash
export GTK_IM_MODULE=<ibus 또는 fcitx>   # GTK 앱 (Firefox, 파일관리자 등)
export QT_IM_MODULE=<ibus 또는 fcitx>    # Qt 앱 (VS Code, KDE 앱 등)
export XMODIFIERS=@im=<ibus 또는 fcitx>  # X Window 전체
```

### 4. 트러블슈팅 체크 순서
1. `locale` → `ko_KR.UTF-8` 확인
2. `fc-list | grep -i nanum` → 한글 폰트 설치 확인
3. `cat ~/.xsessionrc` → 환경변수 3개 확인
4. 입력기 프로세스 실행 확인 (`ibus daemon` 또는 `fcitx -d`)
5. 입력기 설정에서 Hangul 추가 확인
